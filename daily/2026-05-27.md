# 工作日志 · 2026-05-27 — L1.6 改密/重置密码后强制下线

> 涉及 repo：yw-mall · yw-mall-docs
> 闭环 5 天前刚交付的"忘记密码"流程留下的最后一个安全口子

---

## 📌 当前状态

- `ChangePassword` 和 `ResetPasswordByCode` 两个 RPC 成功改密后，会按 role 清掉 `user_sessions:{uid}` 里对应身份的所有 session
- "改完密码旧 token 还能用 30 分钟"的窗口期彻底关掉
- 不动 proto、不动 index 布局，纯加一个 helper + 两处 3 行调用
- `login-revamp.md` 的 P1 跟踪表：L1.6 ⬜ → ✅

---

## 🔧 主要交付

### 1. 新增 helper：`destroyUserSessionsByRole`

文件：`mall-user-rpc/internal/logic/sessionhelpers.go`（+47 行）

```go
func destroyUserSessionsByRole(ctx context.Context, svcCtx *svc.ServiceContext,
                               uid int64, role string) (int, error) {
    if uid <= 0 || role == "" {
        return 0, nil
    }
    tokens, err := svcCtx.Redis.SMembers(ctx, userSessionsKey(uid)).Result()
    if err != nil && err != redis.Nil {
        return 0, err
    }
    removed := 0
    for _, access := range tokens {
        raw, gerr := svcCtx.Redis.Get(ctx, sessionKey(access)).Result()
        if gerr != nil {
            // 索引还在但 session 已过期 — 顺手把悬挂条目也清掉
            _ = svcCtx.Redis.SRem(ctx, userSessionsKey(uid), access).Err()
            continue
        }
        var sess sessionPayload
        if jerr := json.Unmarshal([]byte(raw), &sess); jerr != nil {
            continue
        }
        if sess.Role != role {                    // ← role 过滤是关键
            continue
        }
        if sess.RefreshToken != "" {
            _ = svcCtx.Redis.Del(ctx, refreshKey(sess.RefreshToken)).Err()
        }
        _ = svcCtx.Redis.Del(ctx, sessionKey(access)).Err()
        _ = svcCtx.Redis.SRem(ctx, userSessionsKey(uid), access).Err()
        removed++
    }
    return removed, nil
}
```

跟现有 `DestroyAllUserSessionsLogic.DestroyAllUserSessions` 的区别只有一点：**按 `sess.Role == role` 过滤**。

### 2. `ChangePassword` 收尾调用

文件：`mall-user-rpc/internal/logic/changepasswordlogic.go`（+13 行，第 8 步）

```go
// 8. Force-logout
role := "user"
if subjectType == subjectTypeAdmin {
    role = "admin"
}
if n, derr := destroyUserSessionsByRole(l.ctx, l.svcCtx, in.SubjectId, role); derr != nil {
    l.Logger.Errorf("ChangePassword: destroyUserSessionsByRole(uid=%d,role=%s) failed: %v",
        in.SubjectId, role, derr)
} else if n > 0 {
    l.Logger.Infof("ChangePassword: kicked %d %s session(s) for uid=%d",
        n, role, in.SubjectId)
}
```

- Best-effort 语义：Redis 操作失败 → 日志报错，**不回滚密码改动**（用户已经能用新密码登录了）
- `subjectType` 1 → role `"user"`、2 → role `"admin"`，跟 `ChangePassword` 现有 admin/c-user 分支保持一致

### 3. `ResetPasswordByCode` 收尾调用

文件：`mall-user-rpc/internal/logic/resetpasswordbycodelogic.go`（+9 行）

`role` 固定 `"user"`（c 端忘记密码入口，不可能命中 admin）。

---

## 🤔 为什么按 `role` 过滤，不直接调 `DestroyAllUserSessions`

读 `sessionhelpers.go` 注释 + `CreateSession` 代码确认：

```go
// user_sessions:{uid} SET<access_token>     no TTL
```

```go
// createsessionlogic.go
if err := l.svcCtx.Redis.SAdd(l.ctx, userSessionsKey(in.Uid), access).Err(); ...
```

**`CreateSession` 用的 key 只跟 `in.Uid` 走，不掺 role**。Admin 登录和 c-user 登录都通过同一个 `CreateSession` 路径写到这个 set。

所以现实里：
- `admin_user.id = 1`（种子 admin） + `user.id = 1`（alice）**共享** `user_sessions:1` 这一个 Redis set
- admin 改密时若一锅端调 `DestroyAllUserSessions(1)`：alice 也被踢下线 ❌

按 `sess.Role` 过滤是**不动 proto / 不动 index 布局**的最小侵入修法。代价是每次多一次 GET session JSON，但 N 个 token 也就 N 次 Redis lookup，可忽略。

未来如果要彻底解掉这个 uid 共享 set 的问题，正路是把 key 从 `user_sessions:{uid}` 改成 `user_sessions:{role}:{uid}`，**所有写入和读取点同步迁移**，那是 P1+ 单独工程，不在本次范围。

---

## 🐛 排错记录

| 现象 | 根因 | 修法 |
|---|---|---|
| gopls 一直报 `cannot find package "mall-user-rpc/internal/svc" in GOROOT` | 多 module 项目没配 `go.work`，gopls 不认 sub-module 的 `go.mod` replace | 不修 — `go build ./...` 是真相，gopls 只是 IDE 噪声 |

---

## ✅ 验证清单

### 静态
```
$ cd ~/workspace/go/mall/yw-mall/mall-user-rpc && go build ./...
✓ Success
```

### E2E（同日跑完，全过）

**setup**：注册一个新 c-user `e2e14775` (uid=15) → 登录两次拿 T1+T2 → Redis 注入一个伪造的 `role=admin` session（FAKE_ADMIN_TOKEN）到 `user_sessions:15`，模拟 admin_user.id 与 user.id 共享同一 uid 的真实场景。

**ChangePassword 路径**：
| 验证点 | 实测 |
|---|---|
| `POST /api/user/password` 返回 | `{"ok":true}` |
| T1 → `GET /api/user/data/scope` | HTTP **401** ✅ |
| T2 → `GET /api/user/data/scope` | HTTP **401** ✅ |
| `session:FAKE_ADMIN_TOKEN` 是否还在 | EXISTS = **1** ✅ |
| `refresh:FAKE_REFRESH_TOKEN_e2e` 是否还在 | EXISTS = **1** ✅ |
| `session:T1` / `session:T2` 是否被删 | EXISTS = **0 / 0** ✅ |
| `user_sessions:15` 剩余 | 只剩 `FAKE_ADMIN_TOKEN` ✅ |
| user-rpc 日志 | `ChangePassword: kicked 2 user session(s) for uid=15` ✅ |

**ResetPasswordByCode 路径**：
| 验证点 | 实测 |
|---|---|
| 新密码登录拿 T3，T3 → 受保护接口 | 200（baseline） |
| `POST /api/auth/send-code` scene=4 | 返 `challengeToken` + dev `devCode` ✅ |
| `POST /api/auth/reset-password` 返回 | `{"ok":true}` |
| T3 → `GET /api/user/data/scope` | HTTP **401** ✅ |
| `user_sessions:15` 剩余 | 只剩 `FAKE_ADMIN_TOKEN` ✅ |
| FAKE_ADMIN session + refresh 仍存活 | EXISTS = **1 / 1** ✅ |
| user-rpc 日志 | `ResetPasswordByCode: kicked 1 user session(s) for uid=15` ✅ |
| 用 reset 后的新密码重新登录 | 成功，`expiresIn=1800` ✅ |

**结论**：role filter 工作正常——`user` 的密码改动只清掉 `sessionPayload.Role == "user"` 的条目，admin 同 uid 的 session 不受波及。

### 跑测时遇到的小污染（不影响功能验证）

- alice (id=1) 的 `phone_hash` 是用旧 `MALL_FIELD_ENCRYPTION_KEY` 算的，与当前 env 的默认 key 不匹配 → 走 alice 路径的 reset-password 会返 "该手机号未注册"。改测策略：用 Redis 注入假 admin session 来精确验证 role 过滤，比依赖 seed 数据干净。
- `user_sessions:1` 残留 23 个早期测试遗留的 token——helper 已经把 "索引在但 session 已 expired" 的悬挂条目顺手清掉的逻辑覆盖了这类场景。

### 重现脚本

```bash
# 1. 注册
PHONE=13900001234; SUFFIX=$(date +%s | tail -c 6); USERNAME="e2e$SUFFIX"; PWD="Pass1234!"
curl -s -X POST http://localhost:18888/api/auth/send-code -H 'Content-Type: application/json' \
  -d "{\"channel\":1,\"target\":\"$PHONE\",\"scene\":1}" > /tmp/sc.json
TOKEN=$(jq -r .challengeToken /tmp/sc.json); CODE=$(jq -r .devCode /tmp/sc.json)
curl -s -X POST http://localhost:18888/api/auth/register -H 'Content-Type: application/json' \
  -d "{\"username\":\"$USERNAME\",\"password\":\"$PWD\",\"phone\":\"$PHONE\",\"verifyCode\":\"$CODE\",\"challengeToken\":\"$TOKEN\"}"

# 2. 双设备登录
T1=$(curl -s -X POST http://localhost:18888/api/auth/login -H 'Content-Type: application/json' \
  -d "{\"account\":\"$USERNAME\",\"password\":\"$PWD\"}" | jq -r .accessToken)
# ... 重复一次拿 T2

# 3. 注入假 admin session（替换 UID_E2E 为实际 uid）
podman exec redis-master redis-cli SET "session:FAKE_ADMIN" \
  '{"uid":15,"role":"admin","csrfToken":"x","loginTime":0,"lastActive":0,"refreshToken":"FAKE_REF"}' EX 1800
podman exec redis-master redis-cli SADD user_sessions:15 "FAKE_ADMIN"

# 4. 改密
curl -X POST -H "Authorization: Bearer $T1" -H 'Content-Type: application/json' \
  -d "{\"oldPassword\":\"$PWD\",\"newPassword\":\"NewP@ss2026\"}" \
  http://localhost:18888/api/user/password

# 5. 验证 T1/T2 → 401，FAKE_ADMIN 仍 EXISTS=1
curl -o /dev/null -w "%{http_code}\n" -H "Authorization: Bearer $T1" http://localhost:18888/api/user/data/scope
podman exec redis-master redis-cli EXISTS session:FAKE_ADMIN
```

---

## 📝 改动文件

| Repo | File | 改动 |
|---|---|---|
| yw-mall | `mall-user-rpc/internal/logic/sessionhelpers.go` | +47 行 — `destroyUserSessionsByRole` |
| yw-mall | `mall-user-rpc/internal/logic/changepasswordlogic.go` | +13 行 — step 8 force-logout |
| yw-mall | `mall-user-rpc/internal/logic/resetpasswordbycodelogic.go` | +9 行 — 收尾 force-logout |
| yw-mall-docs | `feat/login-revamp.md` | L1.6 ⬜ → ✅ (2026-05-27) |
| yw-mall-docs | `daily/2026-05-27.md` | 本文 |

---

## 🚧 已知遗留 / 下一步

### 小尾巴（本次范围外）
- ~~e2e 手工跑~~ ✅ 同日跑完（见上面「✅ 验证清单」）
- 前端拦截：c 端 `request.ts` 已有 401 → refresh 单飞逻辑，refresh 也会失效，二次失败 → 跳登录页，刚好顺势接住，**不用动 FE 代码**（已在 e2e 链路里间接验证）
- alice (id=1) 的 phone_hash 与当前 env 的 `MALL_FIELD_ENCRYPTION_KEY` 不匹配——可能需要 backfill 重算（属于历史污染，跟 L1.6 无关，单独归档）

### 长尾（P1+ 候选）
- `user_sessions:{uid}` 改造为 `user_sessions:{role}:{uid}`：彻底干掉 admin/c-user uid 碰撞风险；CreateSession + DestroySession + RefreshSession + DestroyAllUserSessions 全部要同步改 key，需要数据迁移脚本（旧 set 的 token 按 sess.Role 重新分桶到新 set），算半 sprint 工程
- L1.3 登录限流（同 IP 5 分钟 5 次失败锁 30 min）：跟 L1.6 同属安全底线、1 天工程，下个 session 候选
- L1.5 我的设备页：依赖 user_sessions:{uid} 列出所有 session + 一键踢出，3 天，前后端都要碰

---

## 当日总体验收

- 把 5 天前忘记密码后端留下的"30 分钟旧 token 仍可用"安全口子关掉
- `ChangePassword` 同时受益（admin 和 c-user 路径）
- 修法最小侵入：1 个 helper + 2 处 3 行调用，无 proto 变更、无 Redis key 布局变更
- **同日 e2e 跑通**：双设备 login + 注入假 admin session → 改密 → T1/T2 → 401 + admin 保留；reset-password 同样过。user-rpc 日志精确打印 `kicked N user session(s)`
- `login-revamp.md` P1 第一项打勾 — L1.6 ✅
